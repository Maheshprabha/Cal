package com.atmecs.calculator;

import java.util.ArrayList;
import java.util.Scanner;

public class Addition {

	public void addingList() {
		ArrayList<Integer>number = new ArrayList<Integer>();
	   int choice;
	if(choice > 0 && choice<=4) {
		Scanner input = new Scanner(system.in);
		System.out.println("enter the number of valuesto want perform");
		int num = input.hasNextInt();
		System.out.println("enter" +num+"inputs:");
		for(int i=0;i<num;i++) {
			int arraylist = input.hasNextInt();
			number.add(arraylist);
		}
		}
		   
	   
		
		
		/*
		 * //To get the input from user ArrayList<Integer>arrlist = new
		 * ArrayList<Integer>(); Scanner scan = new Scanner(System.in);
		 * //System.out.println("Enter the size of the array :");
		 * System.out.println("Enter the elements :"); while(scan.hasNextInt()) {
		 * arrlist.add(scan.nextInt()); } System.out.println(arrlist);
		 */			
			 
		 }
	 

}
